query {
    getTransaction(transactionId: "{transactionId}"){
        transactionId,
        total{
            currency,
            value
        }
        payments {
            paymentId
            status
            money{
                currency
                value
            }
        }
    }
}